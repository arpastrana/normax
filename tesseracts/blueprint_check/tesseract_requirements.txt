# LGPL-2.1, experiment-only, waived 2026-08-15: never on the submission path.
# Resolves from PyPI inside any build context — no wheel TODO, unlike ec3_check.
blue-prints>=0.7.1
# Annotations only; jaxtyping imports no jax, so the no-JAX claim holds.
jaxtyping>=0.3.11
