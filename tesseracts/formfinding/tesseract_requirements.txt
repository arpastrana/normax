# Built by `uv build` at the repo root, and gitignored, so build the wheel
# before `tesseract build`. The version is the one in pyproject.toml.
../../dist/normax-0.1-py3-none-any.whl
jax-fdm>=0.14.0
