# The gridshell of experiment 21, drawn exactly as its generator builds it.
# Units are millimeters and newtons throughout.

structure:
  num_rings: 4
  num_spokes: 12
  radius: 5000.0  # millimeters
  rise: 2000.0  # millimeters

sections:
  section_class: 3
  # One frame per seed, named apart in the panel; switch frames to compare.
  seed_diameters:
    - 100.0
    - 250.0

viewer:
  enabled: true
